# AI Banking Risk Platform — Root Requirements
# Covers all 16 chapters. Individual chapters may pin tighter versions;
# see each chapter's own requirements.txt for chapter-specific dependencies.
#
# Install: pip install -r requirements.txt
# Python 3.11+ required

# ── Web / API ──────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
httpx>=0.26.0
python-jose[cryptography]==3.3.0

# ── Data & validation ─────────────────────────────────────────────────────
pydantic>=2.6.0
pydantic-settings>=2.2.0
pandas>=2.1.0
numpy>=1.26.0
scipy>=1.11.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9
redis>=5.0.0
boto3>=1.34.0

# ── Machine learning ──────────────────────────────────────────────────────
scikit-learn>=1.4.0
xgboost>=2.0.0
lightgbm>=4.3.0
shap>=0.44.0
optuna>=3.6.0

# ── LLM / RAG / Agentic ───────────────────────────────────────────────────
google-generativeai>=0.8.0
chromadb>=0.5.0
rank-bm25>=0.2.2
pymupdf>=1.24.0

# ── MLOps / Data Infrastructure ───────────────────────────────────────────
mlflow>=2.13.0
dvc[s3]>=3.49.0
apache-airflow>=2.9.0
openlineage-airflow>=1.19.0
great-expectations>=0.18.0

# ── Document processing ───────────────────────────────────────────────────
python-docx>=1.1.0

# ── Testing ───────────────────────────────────────────────────────────────
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
